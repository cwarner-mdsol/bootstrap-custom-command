---
- hosts: "{{ calling_host }}"
  gather_facts: false
  tasks:
    - name: Running bootstrap requirements
      shell: /bin/echo "Working via git" > /tmp/blah
      become: yes
      become_user: root

